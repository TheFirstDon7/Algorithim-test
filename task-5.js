var fullName = "Casey Sam Aigbirio ";

fullName = fullName.trim(); //use .trim to remove all whitespace

fullName = fullName.replaceAll(" ", "%20"); // use.replace all to replace all whitespace with %20

console.log(fullName);